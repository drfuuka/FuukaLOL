import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  animatedContainer: {
    position: 'absolute',
    left: 0,
    top: 0,
  },
  image: {
    width: '100%',
    height: '100%',
  },
});

export default styles;
