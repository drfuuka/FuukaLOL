import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  textWrapper: {
    position: 'absolute',
  },
  text: {
    fontSize: 24,
    color: '#000',
    textAlign: 'center',
  },
});

export default styles;
